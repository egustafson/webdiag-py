services:
  web:
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "5000:5000"
    environment:
      - PYTHONPATH=/app
    command: ["flask", "run", "--host=0.0.0.0", "--port=5000"]
